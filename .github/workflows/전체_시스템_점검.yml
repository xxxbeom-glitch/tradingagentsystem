name: 전체 시스템 점검

on:
  workflow_dispatch:
    inputs:
      ai_테스트_포함:
        description: 'AI 에이전트 테스트 포함 여부 (팀A/B 실제 호출 — 토큰 소모)'
        required: true
        default: '아니오'
        type: choice
        options:
          - '예'
          - '아니오'

jobs:
  full-check:
    runs-on: ubuntu-latest

    env:
      DART_API_KEY: ${{ secrets.DART_API_KEY }}
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      DEEPSEEK_BASE_URL: ${{ secrets.DEEPSEEK_BASE_URL }}
      DEEPSEEK_MODEL: ${{ secrets.DEEPSEEK_MODEL }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      KIS_ACCOUNT_NO: ${{ secrets.KIS_ACCOUNT_NO }}
      KIS_APP_KEY: ${{ secrets.KIS_APP_KEY }}
      KIS_APP_SECRET: ${{ secrets.KIS_APP_SECRET }}
      KRX_ID: ${{ secrets.KRX_ID }}
      KRX_PW: ${{ secrets.KRX_PW }}
      NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
      NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
      KIS_BASE_URL: https://openapi.koreainvestment.com:9443

    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4

      - name: Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 패키지 설치
        run: pip install -r requirements.txt

      - name: 01 환경변수 체크
        id: check-env
        run: |
          python - << 'EOF'
          import os

          keys = [
              "DART_API_KEY", "DEEPSEEK_API_KEY", "DEEPSEEK_BASE_URL", "DEEPSEEK_MODEL",
              "GEMINI_API_KEY", "KIS_ACCOUNT_NO", "KIS_APP_KEY", "KIS_APP_SECRET",
              "KRX_ID", "KRX_PW", "NAVER_CLIENT_ID", "NAVER_CLIENT_SECRET",
          ]
          all_ok = True
          for key in keys:
              ok = len(os.environ.get(key, "")) > 0
              print(f"[{'OK' if ok else 'FAIL'}] {key}")
              if not ok:
                  all_ok = False
          if not all_ok:
              exit(1)
          print("환경변수 체크 통과")
          EOF

      - name: KIS API 토큰 발급 테스트
        id: test-kis
        run: |
          python - << 'EOF'
          import os, json, requests, time
          from datetime import datetime

          app_key = os.environ["KIS_APP_KEY"]
          app_secret = os.environ["KIS_APP_SECRET"]
          url = "https://openapi.koreainvestment.com:9443/oauth2/tokenP"

          start = time.time()
          try:
              res = requests.post(url, json={
                  "grant_type": "client_credentials",
                  "appkey": app_key,
                  "appsecret": app_secret
              }, timeout=10)
              latency = int((time.time() - start) * 1000)
              ok = res.status_code == 200 and "access_token" in res.json()
              result = {
                  "status": "ok" if ok else "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "토큰발급": "성공" if ok else "실패",
                      "응답코드": str(res.status_code),
                      "계좌번호확인": "성공" if os.environ.get("KIS_ACCOUNT_NO") else "누락",
                      "응답시간": f"{latency}ms"
                  },
                  "error": None if ok else f"HTTP {res.status_code}: {res.text[:100]}"
              }
              print(f"[{'OK' if ok else 'FAIL'}] KIS API 토큰 발급: {latency}ms")
              token = res.json().get("access_token", "") if ok else ""
              with open("_kis_token.txt", "w") as f:
                  f.write(token)
          except Exception as e:
              result = {
                  "status": "fail", "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {"토큰발급": "실패", "응답코드": "—", "계좌번호확인": "—", "응답시간": "—"},
                  "error": str(e)
              }
              print(f"[FAIL] KIS API: {e}")

          with open("_kis_result.json", "w") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF

      - name: DART API 테스트
        id: test-dart
        run: |
          python - << 'EOF'
          import os, json, requests, time
          from datetime import datetime, timedelta

          api_key = os.environ["DART_API_KEY"]
          url = "https://opendart.fss.or.kr/api/list.json"
          today = datetime.now()
          end_de = today.strftime("%Y%m%d")
          bgn_de = (today - timedelta(days=30)).strftime("%Y%m%d")

          start = time.time()
          try:
              res = requests.get(url, params={
                  "crtfc_key": api_key,
                  "bgn_de": bgn_de,
                  "end_de": end_de,
                  "page_count": 5
              }, timeout=10)
              latency = int((time.time() - start) * 1000)
              data = res.json()
              ok = res.status_code == 200 and data.get("status") == "000"
              count = data.get("total_count", 0) if ok else 0
              result = {
                  "status": "ok" if ok else "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "API키확인": "성공",
                      "공시조회테스트": "성공" if ok else "실패",
                      "조회기간": f"{bgn_de}~{end_de}",
                      "기간내공시건수": f"{count}건",
                      "응답시간": f"{latency}ms"
                  },
                  "error": None if ok else f"status: {data.get('status')} message: {data.get('message')}"
              }
              print(f"[{'OK' if ok else 'FAIL'}] DART API: {count}건 {latency}ms")
          except Exception as e:
              result = {
                  "status": "fail", "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {"API키확인": "성공", "공시조회테스트": "실패", "조회기간": f"{bgn_de}~{end_de}", "기간내공시건수": "—", "응답시간": "—"},
                  "error": str(e)
              }
              print(f"[FAIL] DART API: {e}")

          with open("_dart_result.json", "w") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF

      - name: Gemini API 테스트
        id: test-gemini
        run: |
          python - << 'EOF'
          import os, json, time
          from datetime import datetime
          import google.generativeai as genai

          api_key = os.environ["GEMINI_API_KEY"]
          genai.configure(api_key=api_key)

          start = time.time()
          try:
              model = genai.GenerativeModel("gemini-2.5-flash")
              response = model.generate_content("ping")
              latency = int((time.time() - start) * 1000)
              ok = response and len(response.text) > 0
              result = {
                  "status": "ok" if ok else "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "모델": "gemini-2.5-flash",
                      "연결상태": "성공" if ok else "실패",
                      "응답테스트": "성공" if ok else "실패",
                      "응답시간": f"{latency}ms"
                  },
                  "error": None
              }
              print(f"[{'OK' if ok else 'FAIL'}] Gemini API: {latency}ms")
          except Exception as e:
              result = {
                  "status": "fail", "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {"모델": "gemini-2.5-flash", "연결상태": "실패", "응답테스트": "실패", "응답시간": "—"},
                  "error": str(e)
              }
              print(f"[FAIL] Gemini API: {e}")

          with open("_gemini_result.json", "w") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF

      - name: DeepSeek API 테스트
        id: test-deepseek
        run: |
          python - << 'EOF'
          import os, json, time
          from datetime import datetime
          from openai import OpenAI

          api_key = os.environ["DEEPSEEK_API_KEY"]
          base_url = os.environ.get("DEEPSEEK_BASE_URL", "https://api.deepseek.com")
          model = os.environ.get("DEEPSEEK_MODEL", "deepseek-chat")

          start = time.time()
          try:
              client = OpenAI(api_key=api_key, base_url=base_url)
              response = client.chat.completions.create(
                  model=model,
                  messages=[{"role": "user", "content": "ping"}],
                  max_tokens=100
              )
              latency = int((time.time() - start) * 1000)
              ok = response and response.choices[0].message.content
              result = {
                  "status": "ok" if ok else "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "모델": model,
                      "연결상태": "성공" if ok else "실패",
                      "응답테스트": "성공" if ok else "실패",
                      "응답시간": f"{latency}ms"
                  },
                  "error": None
              }
              print(f"[{'OK' if ok else 'FAIL'}] DeepSeek API: {latency}ms")
          except Exception as e:
              result = {
                  "status": "fail", "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {"API키확인": "성공", "연결시도": "실패"},
                  "error": str(e)
              }
              print(f"[FAIL] DeepSeek API: {e}")

          with open("_deepseek_result.json", "w") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF

      - name: Naver API 테스트
        id: test-naver
        run: |
          python - << 'EOF'
          import os, json, requests, time
          from datetime import datetime

          client_id = os.environ["NAVER_CLIENT_ID"]
          client_secret = os.environ["NAVER_CLIENT_SECRET"]
          url = "https://openapi.naver.com/v1/search/news.json"

          start = time.time()
          try:
              res = requests.get(url,
                  headers={"X-Naver-Client-Id": client_id, "X-Naver-Client-Secret": client_secret},
                  params={"query": "코스닥", "display": 5},
                  timeout=10
              )
              latency = int((time.time() - start) * 1000)
              ok = res.status_code == 200
              count = res.json().get("total", 0) if ok else 0
              result = {
                  "status": "ok" if ok else "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "CLIENT_ID확인": "성공",
                      "뉴스검색테스트": "성공" if ok else "실패",
                      "검색결과": f"{count}건",
                      "응답시간": f"{latency}ms"
                  },
                  "error": None if ok else f"HTTP {res.status_code}"
              }
              print(f"[{'OK' if ok else 'FAIL'}] Naver API: {count}건 {latency}ms")
          except Exception as e:
              result = {
                  "status": "fail", "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {"CLIENT_ID확인": "성공", "뉴스검색테스트": "실패", "검색결과": "—", "응답시간": "—"},
                  "error": str(e)
              }
              print(f"[FAIL] Naver API: {e}")

          with open("_naver_result.json", "w") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF

      - name: pykrx 테스트
        id: test-pykrx
        run: |
          python - << 'EOF'
          import json, time
          from datetime import datetime, timedelta

          start = time.time()
          try:
              from pykrx import stock
              today = datetime.now().strftime("%Y%m%d")
              yesterday = (datetime.now() - timedelta(days=1)).strftime("%Y%m%d")
              df = stock.get_market_ohlcv_by_ticker(yesterday)
              latency = int((time.time() - start) * 1000)
              ok = df is not None and len(df) > 0
              result = {
                  "status": "ok" if ok else "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "라이브러리로드": "성공",
                      "코스피조회": "성공" if ok else "실패",
                      "코스닥조회": "성공" if ok else "실패",
                      "종목수": f"{len(df)}개" if ok else "—",
                      "응답시간": f"{latency}ms"
                  },
                  "error": None if ok else "데이터 없음"
              }
              print(f"[{'OK' if ok else 'FAIL'}] pykrx: {len(df) if ok else 0}개 {latency}ms")
          except Exception as e:
              result = {
                  "status": "fail", "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {"라이브러리로드": "실패", "코스피조회": "—", "코스닥조회": "—", "응답시간": "—"},
                  "error": str(e)
              }
              print(f"[FAIL] pykrx: {e}")

          with open("_pykrx_result.json", "w") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF


      - name: KIS 실시간·수급 테스트 (realtime / supply, 토큰 공유)
        id: test-kis-data
        run: |
          python - << 'EOF'
          import json, os, sys, time
          from datetime import datetime

          sys.path.insert(0, os.getcwd())
          from data.realtime import KISClient

          WF = "전체_시스템_점검.yml"
          RUN_AT = datetime.now().strftime("%Y-%m-%dT%H:%M:%S")
          ticker = "005930"

          client = KISClient()
          if os.path.exists("_kis_token.txt"):
              with open("_kis_token.txt") as f:
                  token = f.read().strip()
              if token:
                  client._access_token = token
                  client._token_expires_at = time.time() + 86400
                  print("[INFO] 기존 토큰 재사용")
          print("[INFO] KISClient 단일 인스턴스 생성 — 토큰 1회 발급 후 재사용")

          # --- realtime (현재가) ---
          start = time.time()
          try:
              data = client.get_current_price(ticker)
              latency = int((time.time() - start) * 1000)
              ok = data.get("price", 0) > 0
              realtime_result = {
                  "status": "ok" if ok else "fail",
                  "workflow": WF,
                  "run_at": RUN_AT,
                  "data": {
                      "종목": f"{data.get('name') or '삼성전자'}({ticker})",
                      "현재가": f"{data.get('price', 0):,}원",
                      "등락률": f"{data.get('change_rate', 0)}%",
                      "거래량": f"{data.get('volume', 0):,}",
                      "토큰재사용": "성공",
                      "응답시간": f"{latency}ms",
                  },
                  "error": None,
              }
              print(f"[{'OK' if ok else 'FAIL'}] realtime: {data.get('price')} {latency}ms")
          except Exception as e:
              realtime_result = {
                  "status": "fail",
                  "workflow": WF,
                  "run_at": RUN_AT,
                  "data": {
                      "종목": f"삼성전자({ticker})",
                      "현재가": "—",
                      "등락률": "—",
                      "거래량": "—",
                      "토큰재사용": "—",
                      "응답시간": "—",
                  },
                  "error": str(e),
              }
              print(f"[FAIL] realtime: {e}")

          with open("_realtime_result.json", "w", encoding="utf-8") as f:
              json.dump(realtime_result, f, ensure_ascii=False)

          # --- supply (수급 가집계, 동일 client) ---
          start = time.time()
          try:
              data = client.get_net_buy_aggregate()
              latency = int((time.time() - start) * 1000)
              foreign = data.get("foreign_net", 0)
              institution = data.get("institution_net", 0)
              supply_result = {
                  "status": "ok",
                  "workflow": WF,
                  "run_at": RUN_AT,
                  "data": {
                      "외국인순매수조회": "성공",
                      "기관순매수조회": "성공",
                      "외국인순매수": f"{foreign:,}",
                      "기관순매수": f"{institution:,}",
                      "토큰재사용": "성공",
                      "응답시간": f"{latency}ms",
                  },
                  "error": None,
              }
              print(f"[OK] supply: foreign={foreign} institution={institution} {latency}ms")
          except Exception as e:
              supply_result = {
                  "status": "fail",
                  "workflow": WF,
                  "run_at": RUN_AT,
                  "data": {
                      "외국인순매수조회": "실패",
                      "기관순매수조회": "실패",
                      "외국인순매수": "—",
                      "기관순매수": "—",
                      "토큰재사용": "—",
                      "응답시간": "—",
                  },
                  "error": str(e),
              }
              print(f"[FAIL] supply: {e}")

          with open("_supply_result.json", "w", encoding="utf-8") as f:
              json.dump(supply_result, f, ensure_ascii=False)
          EOF

      - name: 트리거 조건 테스트
        id: test-trigger
        run: |
          python - << 'EOF'
          import json, os, sys
          from datetime import datetime

          sys.path.insert(0, os.getcwd())
          from triggers.conditions import (
              check_volume_surge,
              check_institution_buy_turn,
              check_foreign_consecutive_buy,
              check_sector_top3,
              check_52week_high,
              count_triggers,
          )

          WF = "전체_시스템_점검.yml"
          RUN_AT = datetime.now().strftime("%Y-%m-%dT%H:%M:%S")

          results = []
          all_ok = True

          r1 = check_volume_surge("005930", 3000000, 900000)
          ok = r1["triggered"] == True
          results.append(r1)
          if not ok: all_ok = False
          print(f"[{'OK' if ok else 'FAIL'}] check_volume_surge: {r1}")

          r2 = check_institution_buy_turn("005930", 50000, -30000)
          ok = r2["triggered"] == True
          results.append(r2)
          if not ok: all_ok = False
          print(f"[{'OK' if ok else 'FAIL'}] check_institution_buy_turn: {r2}")

          r3 = check_foreign_consecutive_buy("005930", [100000, 200000, 150000])
          ok = r3["triggered"] == True
          results.append(r3)
          if not ok: all_ok = False
          print(f"[{'OK' if ok else 'FAIL'}] check_foreign_consecutive_buy: {r3}")

          r4 = check_sector_top3(2)
          ok = r4["triggered"] == True
          results.append(r4)
          if not ok: all_ok = False
          print(f"[{'OK' if ok else 'FAIL'}] check_sector_top3: {r4}")

          r5 = check_52week_high("005930", 98000, 100000)
          ok = r5["triggered"] == True
          results.append(r5)
          if not ok: all_ok = False
          print(f"[{'OK' if ok else 'FAIL'}] check_52week_high: {r5}")

          count = count_triggers(results)
          print(f"[OK] count_triggers: {count}개 충족")

          result = {
              "status": "ok" if all_ok else "fail",
              "workflow": WF,
              "run_at": RUN_AT,
              "data": {
                  "거래량급증": "성공" if r1["triggered"] else "실패",
                  "기관순매수전환": "성공" if r2["triggered"] else "실패",
                  "외국인연속순매수": "성공" if r3["triggered"] else "실패",
                  "업종상위3위": "성공" if r4["triggered"] else "실패",
                  "52주신고가": "성공" if r5["triggered"] else "실패",
                  "충족조건수": f"{count}개",
                  "AI호출대상": "예" if count >= 2 else "아니오",
              },
              "error": None,
          }

          with open("_trigger_result.json", "w", encoding="utf-8") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF

      - name: 공시 수집 테스트 (disclosure)
        id: test-disclosure
        run: |
          python - << 'EOF'
          import json, os, sys, time
          from datetime import datetime

          sys.path.insert(0, os.getcwd())
          from data.dart import get_disclosures_by_ticker

          ticker = "005930"
          start = time.time()
          try:
              items = get_disclosures_by_ticker(ticker, days=7)
              latency = int((time.time() - start) * 1000)
              ok = isinstance(items, list)
              count = len(items)
              latest = items[0]["title"][:40] if items else "—"
              result = {
                  "status": "ok" if ok else "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "공시수집": "성공" if ok else "실패",
                      "종목": f"삼성전자({ticker})",
                      "조회건수": f"{count}건",
                      "최근공시": latest,
                      "응답시간": f"{latency}ms",
                  },
                  "error": None,
              }
              print(f"[{'OK' if ok else 'FAIL'}] disclosure: {count}건 {latency}ms")
          except Exception as e:
              result = {
                  "status": "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "공시수집": "실패",
                      "종목": f"삼성전자({ticker})",
                      "조회건수": "—",
                      "최근공시": "—",
                      "응답시간": "—",
                  },
                  "error": str(e),
              }
              print(f"[FAIL] disclosure: {e}")

          with open("_disclosure_result.json", "w", encoding="utf-8") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF


      - name: SQLite 연결 테스트 (db-connect)
        id: test-db-connect
        run: |
          python - << 'EOF'
          import json, os, sqlite3, time
          from datetime import datetime

          from init_db import init_db, DB_PATH

          if os.path.exists(DB_PATH):
              os.remove(DB_PATH)

          start = time.time()
          try:
              init_db()
              latency = int((time.time() - start) * 1000)
              exists = os.path.exists(DB_PATH)
              conn = sqlite3.connect(DB_PATH)
              conn.execute("SELECT 1")
              conn.close()
              ok = exists
              result = {
                  "status": "ok" if ok else "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "DB파일생성": "성공" if exists else "실패",
                      "연결상태": "성공" if ok else "실패",
                      "경로": DB_PATH,
                      "응답시간": f"{latency}ms",
                  },
                  "error": None,
              }
              print(f"[{'OK' if ok else 'FAIL'}] db-connect: {DB_PATH} {latency}ms")
          except Exception as e:
              result = {
                  "status": "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "DB파일생성": "실패",
                      "연결상태": "실패",
                      "경로": DB_PATH,
                      "응답시간": "—",
                  },
                  "error": str(e),
              }
              print(f"[FAIL] db-connect: {e}")

          with open("_db_connect_result.json", "w", encoding="utf-8") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF

      - name: 테이블 스키마 테스트 (db-schema)
        id: test-db-schema
        run: |
          python - << 'EOF'
          import json, os, sqlite3, time
          from datetime import datetime

          from init_db import DB_PATH

          expected = [
              "market_data",
              "disclosure_data",
              "trigger_log",
              "ai_decision_log",
              "portfolio",
              "balance",
          ]

          start = time.time()
          try:
              conn = sqlite3.connect(DB_PATH)
              cursor = conn.cursor()
              cursor.execute(
                  "SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%'"
              )
              tables = {row[0] for row in cursor.fetchall()}
              conn.close()
              latency = int((time.time() - start) * 1000)

              checks = {t: ("성공" if t in tables else "실패") for t in expected}
              ok = all(v == "성공" for v in checks.values())

              data = {f"{t}테이블": checks[t] for t in expected}
              data["응답시간"] = f"{latency}ms"

              result = {
                  "status": "ok" if ok else "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": data,
                  "error": None if ok else f"누락 테이블: {', '.join(t for t in expected if t not in tables)}",
              }
              print(f"[{'OK' if ok else 'FAIL'}] db-schema: {checks} {latency}ms")
          except Exception as e:
              result = {
                  "status": "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {f"{t}테이블": "—" for t in expected},
                  "error": str(e),
              }
              print(f"[FAIL] db-schema: {e}")

          with open("_db_schema_result.json", "w", encoding="utf-8") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF

      - name: DB 쓰기/읽기 테스트 (db-write)
        id: test-db-write
        run: |
          python - << 'EOF'
          import json, os, sqlite3, time
          from datetime import datetime

          from init_db import DB_PATH

          now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
          ticker = "005930"
          price = 70000

          start = time.time()
          try:
              conn = sqlite3.connect(DB_PATH)
              cursor = conn.cursor()
              cursor.execute(
                  """
                  INSERT INTO market_data
                      (collected_at, ticker, name, current_price, volume)
                  VALUES (?, ?, ?, ?, ?)
                  """,
                  (now, ticker, "삼성전자", price, 1000),
              )
              conn.commit()
              cursor.execute(
                  """
                  SELECT current_price, name FROM market_data
                  WHERE ticker = ? ORDER BY id DESC LIMIT 1
                  """,
                  (ticker,),
              )
              row = cursor.fetchone()
              conn.close()
              latency = int((time.time() - start) * 1000)

              ok = row is not None and row[0] == price and row[1] == "삼성전자"
              result = {
                  "status": "ok" if ok else "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "INSERT테스트": "성공",
                      "SELECT테스트": "성공" if row else "실패",
                      "데이터일치여부": "성공" if ok else "실패",
                      "저장가격": f"{row[0]:,}원" if row else "—",
                      "소요시간": f"{latency}ms",
                  },
                  "error": None if ok else "INSERT/SELECT 데이터 불일치",
              }
              print(f"[{'OK' if ok else 'FAIL'}] db-write: {latency}ms")
          except Exception as e:
              result = {
                  "status": "fail",
                  "workflow": "전체_시스템_점검.yml",
                  "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
                  "data": {
                      "INSERT테스트": "실패",
                      "SELECT테스트": "—",
                      "데이터일치여부": "—",
                      "저장가격": "—",
                      "소요시간": "—",
                  },
                  "error": str(e),
              }
              print(f"[FAIL] db-write: {e}")

          with open("_db_write_result.json", "w", encoding="utf-8") as f:
              json.dump(result, f, ensure_ascii=False)
          EOF


      - name: 팀 A 에이전트 테스트 (Gemini Flash → DeepSeek R1)
        if: ${{ github.event.inputs.ai_테스트_포함 == '예' }}
        id: gemini-analysis-team-a
        run: |
          python - << 'EOF'
          import json, os, sys, time
          from datetime import datetime

          sys.path.insert(0, os.getcwd())
          from agents.team_a import analyze_with_gemini

          stock_data = {
              "ticker": "005930",
              "name": "삼성전자",
              "current_price": 58000,
              "volume": 3000000,
              "change_rate": 2.5,
              "foreign_net": 500000,
              "institution_net": 200000,
              "market_direction": "우호적",
              "triggers": ["거래량급증", "외국인순매수전환"],
              "disclosures": ["2024-05-25 주식등의대량보유상황보고서"],
              "portfolio": [],
              "available_cash": 500000,
          }

          start = time.time()
          try:
              result = analyze_with_gemini(stock_data)
              latency = int((time.time() - start) * 1000)
              print(f"[OK] Gemini 분석 완료: {latency}ms")
              print(f"결과: {result[:200]}")
              with open("_team_a_gemini_analysis.txt", "w", encoding="utf-8") as f:
                  f.write(result)
              with open("_team_a_gemini_result.json", "w", encoding="utf-8") as f:
                  json.dump({
                      "status": "ok",
                      "latency": f"{latency}ms",
                      "result_preview": result[:200],
                  }, f, ensure_ascii=False)
          except Exception as e:
              print(f"[FAIL] Gemini 분석 실패: {e}")
              with open("_team_a_gemini_result.json", "w", encoding="utf-8") as f:
                  json.dump({"status": "fail", "error": str(e)}, f, ensure_ascii=False)
              exit(1)
          EOF

      - name: DeepSeek R1 — 종목 최종 결정
        if: ${{ github.event.inputs.ai_테스트_포함 == '예' }}
        id: deepseek-decision-team-a
        run: |
          python - << 'EOF'
          import json, os, sys, time
          from datetime import datetime

          sys.path.insert(0, os.getcwd())
          from agents.team_a import decide_with_deepseek

          stock_data = {
              "ticker": "005930",
              "name": "삼성전자",
              "current_price": 58000,
              "available_cash": 500000,
          }

          with open("_team_a_gemini_analysis.txt", "r", encoding="utf-8") as f:
              gemini_result = f.read()

          start = time.time()
          try:
              result = decide_with_deepseek(gemini_result, stock_data)
              latency = int((time.time() - start) * 1000)
              print(f"[OK] DeepSeek 결정 완료: {latency}ms")
              print(f"action={result.get('action')} confidence={result.get('confidence')}")
              with open("_team_a_deepseek_result.json", "w", encoding="utf-8") as f:
                  json.dump({
                      "status": "ok",
                      "latency": f"{latency}ms",
                      "action": result.get("action"),
                      "confidence": result.get("confidence"),
                      "result": result,
                  }, f, ensure_ascii=False)
          except Exception as e:
              print(f"[FAIL] DeepSeek 결정 실패: {e}")
              with open("_team_a_deepseek_result.json", "w", encoding="utf-8") as f:
                  json.dump({"status": "fail", "error": str(e)}, f, ensure_ascii=False)
              exit(1)
          EOF

      - name: JSON 출력 파싱 — action / ticker / 수량 (팀 A)
        if: ${{ github.event.inputs.ai_테스트_포함 == '예' }}
        id: parse-output-team-a
        run: |
          python - << 'EOF'
          import json, os, sys
          from datetime import datetime

          with open("_team_a_deepseek_result.json", "r", encoding="utf-8") as f:
              data = json.load(f)

          result = data.get("result", {})
          action = result.get("action", "관망")
          ticker = result.get("ticker", "005930")
          name = result.get("name", "삼성전자")
          quantity = result.get("quantity", 0)
          confidence = result.get("confidence", "LOW")
          reason = result.get("reason", "")
          target_price = result.get("target_price", 0)
          stop_loss = result.get("stop_loss", 0)
          trade_type = result.get("type", "단타")
          scores = result.get("scores", {})

          ok = action in ["매수", "매도", "관망"]
          print(f"[{'OK' if ok else 'FAIL'}] JSON 파싱 완료")
          print(f"action={action} ticker={ticker} name={name}")
          print(f"quantity={quantity} confidence={confidence}")
          print(f"reason={reason}")
          print(f"target_price={target_price} stop_loss={stop_loss}")
          print(f"scores={scores}")

          final = {
              "status": "ok" if ok else "fail",
              "workflow": "06_07_test_teams.yml",
              "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
              "data": {
                  "action": action,
                  "ticker": f"{name}({ticker})",
                  "type": trade_type,
                  "confidence": confidence,
                  "quantity": f"{quantity}주",
                  "target_price": f"{target_price:,}원" if target_price else "—",
                  "stop_loss": f"{stop_loss:,}원" if stop_loss else "—",
                  "reason": reason,
                  "scores": scores,
              },
              "error": None if ok else "action 값 오류",
          }

          with open("_team_a_final.json", "w", encoding="utf-8") as f:
              json.dump(final, f, ensure_ascii=False, indent=2)

          if not ok:
              exit(1)
          EOF

      # ===== 팀 B =====

      - name: 팀 B 에이전트 테스트 (DeepSeek V3 → Gemini Pro + R1 검증)
        if: ${{ github.event.inputs.ai_테스트_포함 == '예' }}
        id: deepseek-analysis-team-b
        run: |
          python - << 'EOF'
          import json, os, sys, time
          from datetime import datetime

          sys.path.insert(0, os.getcwd())
          from agents.team_b import analyze_with_deepseek_v3

          stock_data = {
              "ticker": "005930",
              "name": "삼성전자",
              "current_price": 58000,
              "volume": 3000000,
              "change_rate": 2.5,
              "foreign_net": 500000,
              "institution_net": 200000,
              "market_direction": "우호적",
              "triggers": ["거래량급증", "외국인순매수전환"],
              "disclosures": ["2024-05-25 주식등의대량보유상황보고서"],
              "portfolio": [],
              "available_cash": 500000,
          }

          start = time.time()
          try:
              result = analyze_with_deepseek_v3(stock_data)
              latency = int((time.time() - start) * 1000)
              print(f"[OK] DeepSeek V3 분석 완료: {latency}ms")
              print(f"결과: {result[:200]}")
              with open("_team_b_deepseek_v3_analysis.txt", "w", encoding="utf-8") as f:
                  f.write(result)
              with open("_team_b_deepseek_v3_result.json", "w", encoding="utf-8") as f:
                  json.dump({
                      "status": "ok",
                      "latency": f"{latency}ms",
                      "result_preview": result[:200],
                  }, f, ensure_ascii=False)
          except Exception as e:
              print(f"[FAIL] DeepSeek V3 분석 실패: {e}")
              with open("_team_b_deepseek_v3_result.json", "w", encoding="utf-8") as f:
                  json.dump({"status": "fail", "error": str(e)}, f, ensure_ascii=False)
              exit(1)
          EOF

      - name: Gemini Pro — 종목 결정
        if: ${{ github.event.inputs.ai_테스트_포함 == '예' }}
        id: gemini-decision-team-b
        run: |
          python - << 'EOF'
          import json, os, sys, time
          from datetime import datetime

          sys.path.insert(0, os.getcwd())
          from agents.team_b import decide_with_gemini_pro

          stock_data = {
              "ticker": "005930",
              "name": "삼성전자",
              "current_price": 58000,
              "available_cash": 500000,
          }

          with open("_team_b_deepseek_v3_analysis.txt", "r", encoding="utf-8") as f:
              analysis = f.read()

          start = time.time()
          try:
              result = decide_with_gemini_pro(analysis, stock_data)
              latency = int((time.time() - start) * 1000)
              print(f"[OK] Gemini Pro 결정 완료: {latency}ms")
              print(f"action={result.get('action')} confidence={result.get('confidence')}")
              with open("_team_b_gemini_pro_result.json", "w", encoding="utf-8") as f:
                  json.dump({
                      "status": "ok",
                      "latency": f"{latency}ms",
                      "action": result.get("action"),
                      "confidence": result.get("confidence"),
                      "result": result,
                  }, f, ensure_ascii=False)
          except Exception as e:
              print(f"[FAIL] Gemini Pro 결정 실패: {e}")
              with open("_team_b_gemini_pro_result.json", "w", encoding="utf-8") as f:
                  json.dump({"status": "fail", "error": str(e)}, f, ensure_ascii=False)
              exit(1)
          EOF

      - name: DeepSeek R1 — 반론 검증 (병렬)
        if: ${{ github.event.inputs.ai_테스트_포함 == '예' }}
        id: deepseek-verify-team-b
        run: |
          python - << 'EOF'
          import json, os, sys, time
          from datetime import datetime

          sys.path.insert(0, os.getcwd())
          from agents.team_b import verify_with_deepseek_r1

          stock_data = {
              "ticker": "005930",
              "name": "삼성전자",
          }

          with open("_team_b_gemini_pro_result.json", "r", encoding="utf-8") as f:
              gemini_data = json.load(f)

          gemini_decision = gemini_data.get("result", {})

          start = time.time()
          try:
              result = verify_with_deepseek_r1(gemini_decision, stock_data)
              latency = int((time.time() - start) * 1000)
              print(f"[OK] DeepSeek R1 검증 완료: {latency}ms")
              print(f"verdict={result.get('verdict')} reason={result.get('reason')}")
              with open("_team_b_r1_verify_result.json", "w", encoding="utf-8") as f:
                  json.dump({
                      "status": "ok",
                      "latency": f"{latency}ms",
                      "verdict": result.get("verdict"),
                      "reason": result.get("reason"),
                      "risk_flags": result.get("risk_flags", []),
                      "result": result,
                  }, f, ensure_ascii=False)
          except Exception as e:
              print(f"[FAIL] DeepSeek R1 검증 실패: {e}")
              with open("_team_b_r1_verify_result.json", "w", encoding="utf-8") as f:
                  json.dump({"status": "fail", "error": str(e)}, f, ensure_ascii=False)
              exit(1)
          EOF

      - name: 최종 승인 / 거절 판단
        if: ${{ github.event.inputs.ai_테스트_포함 == '예' }}
        id: final-decision-team-b
        run: |
          python - << 'EOF'
          import json, os, sys
          from datetime import datetime

          sys.path.insert(0, os.getcwd())
          from agents.team_b import final_decision

          with open("_team_b_gemini_pro_result.json", "r", encoding="utf-8") as f:
              gemini_data = json.load(f)
          with open("_team_b_r1_verify_result.json", "r", encoding="utf-8") as f:
              r1_data = json.load(f)

          gemini_result = gemini_data.get("result", {})
          r1_result = r1_data.get("result", {})

          result = final_decision(gemini_result, r1_result)
          print(f"[OK] 최종 판단 완료")
          print(f"action={result.get('action')} verification={result.get('verification')}")
          print(f"risk_flags={result.get('risk_flags', [])}")

          with open("_team_b_final.json", "w", encoding="utf-8") as f:
              json.dump(result, f, ensure_ascii=False, indent=2)
          EOF

      - name: JSON 출력 파싱 — action / ticker / 수량 (팀 B)
        if: ${{ github.event.inputs.ai_테스트_포함 == '예' }}
        id: parse-output-team-b
        run: |
          python - << 'EOF'
          import json, os
          from datetime import datetime

          with open("_team_b_final.json", "r", encoding="utf-8") as f:
              result = json.load(f)

          action = result.get("action", "관망")
          ticker = result.get("ticker", "005930")
          name = result.get("name", "삼성전자")
          quantity = result.get("quantity", 0)
          confidence = result.get("confidence", "LOW")
          reason = result.get("reason", "")
          entry_price = result.get("entry_price")
          target_price = result.get("target_price", 0)
          stop_loss = result.get("stop_loss", 0)
          trade_type = result.get("type", "단타")
          verification = result.get("verification", "—")
          risk_flags = result.get("risk_flags", [])
          scores = result.get("scores", {})

          ok = action in ["매수", "매도", "관망"]
          print(f"[{'OK' if ok else 'FAIL'}] JSON 파싱 완료")
          print(f"action={action} ticker={ticker} name={name}")
          print(f"quantity={quantity} confidence={confidence}")
          print(f"entry_price={entry_price}")
          print(f"verification={verification} risk_flags={risk_flags}")
          print(f"reason={reason}")
          print(f"scores={scores}")

          final = {
              "status": "ok" if ok else "fail",
              "workflow": "06_07_test_teams.yml",
              "run_at": datetime.now().strftime("%Y-%m-%dT%H:%M:%S"),
              "data": {
                  "action": action,
                  "ticker": f"{name}({ticker})",
                  "type": trade_type,
                  "confidence": confidence,
                  "quantity": f"{quantity}주",
                  "entry_price": f"{entry_price:,}원" if entry_price else "—",
                  "target_price": f"{target_price:,}원" if target_price else "—",
                  "stop_loss": f"{stop_loss:,}원" if stop_loss else "—",
                  "verification": verification,
                  "risk_flags": risk_flags,
                  "reason": reason,
                  "scores": scores,
              },
              "error": None if ok else "action 값 오류",
          }

          with open("_team_b_parsed.json", "w", encoding="utf-8") as f:
              json.dump(final, f, ensure_ascii=False, indent=2)

          if not ok:
              exit(1)
          print("[OK] 팀 B 파이프라인 전체 완료")
          EOF

      - name: AI 테스트 결과 통합
        if: ${{ github.event.inputs.ai_테스트_포함 == '예' }}
        run: |
          python - << 'EOF'
          import json, os

          try:
              with open("results.json", "r", encoding="utf-8") as f:
                  data = json.load(f)
          except Exception:
              data = {}

          mapping = {
              "team-a-collect": "_team_a_gemini_result.json",
              "team-a-decision": "_team_a_deepseek_result.json",
              "team-a-json": "_team_a_final.json",
              "team-b-collect": "_team_b_deepseek_v3_result.json",
              "team-b-decision": "_team_b_gemini_pro_result.json",
              "team-b-verify": "_team_b_r1_verify_result.json",
              "team-b-final": "_team_b_final.json",
              "team-b-json": "_team_b_parsed.json",
          }

          for key, fname in mapping.items():
              if os.path.exists(fname):
                  with open(fname, "r", encoding="utf-8") as f:
                      data[key] = json.load(f)
                  os.remove(fname)

          with open("results.json", "w", encoding="utf-8") as f:
              json.dump(data, f, ensure_ascii=False, indent=2)

          print("AI 테스트 결과 통합 완료")
          EOF


      - name: results.json 통합 업데이트
        if: always()
        run: |
          python - << 'EOF'
          import json, os
          from datetime import datetime

          try:
              with open("results.json", "r", encoding="utf-8") as f:
                  data = json.load(f)
          except Exception:
              data = {}

          data["updated_at"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%S")

          mapping = {
              "kis": "_kis_result.json",
              "dart": "_dart_result.json",
              "gemini": "_gemini_result.json",
              "deepseek": "_deepseek_result.json",
              "naver": "_naver_result.json",
              "pykrx": "_pykrx_result.json",
              "realtime": "_realtime_result.json",
              "supply": "_supply_result.json",
              "trigger": "_trigger_result.json",
              "disclosure": "_disclosure_result.json",
              "db-connect": "_db_connect_result.json",
              "db-schema": "_db_schema_result.json",
              "db-write": "_db_write_result.json",
          }

          # timeline 키가 없으면 빈 배열로 초기화 (기존 데이터 유지)
          if "timeline" not in data:
              data["timeline"] = []

          for key, fname in mapping.items():
              if os.path.exists(fname):
                  with open(fname, "r", encoding="utf-8") as f:
                      data[key] = json.load(f)
                  os.remove(fname)

          with open("results.json", "w", encoding="utf-8") as f:
              json.dump(data, f, ensure_ascii=False, indent=2)

          print("results.json 통합 완료")
          EOF

      - name: results.json 커밋
        if: always()
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git clean -fd results.json
          git pull --rebase --autostash origin main
          git add results.json
          git diff --staged --quiet || git commit -m "ci: 전체 시스템 점검 결과 업데이트"
          git push
